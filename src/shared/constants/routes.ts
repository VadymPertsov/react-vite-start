export const ROUTES = {
  home: '/',
  profile: '/profile',
}

export const PUBLIC_ROUTES = [{ label: 'Home', path: ROUTES.home }]

export const PRIVATE_ROUTES = [{ label: 'Profile', path: ROUTES.profile }]
